# Excel template import fixtures

These workbooks are production-shaped regression fixtures for the Excel
template import parser. Keep them unchanged unless the matching assertions in
`src/features/report/lib/excelTemplateImport.fixtures.test.js` are updated
intentionally.

